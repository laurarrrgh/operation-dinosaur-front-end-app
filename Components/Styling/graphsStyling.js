import React, { StyleSheet } from "react-native";

export default (styles = StyleSheet.create({
  graphMainTitle: {
    fontSize: 38,
    fontWeight: "bold",
    margin: 20,
    textAlign: "center"
  },
  graphMain: {
    marginTop: 88,
    marginBottom: 88
  },
  graphTitle: {
    fontSize: 18,
    fontWeight: "bold",
    margin: 20,
    textAlign: "center"
  }
}));
