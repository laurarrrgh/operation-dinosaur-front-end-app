import React, { StyleSheet } from "react-native";

export default (styles = StyleSheet.create({
  graphMain: {
    marginTop: 88
  },
  graphMainTitle: {
    fontSize: 38,
    fontWeight: "bold",
    margin: 10,
    textAlign: "center"
  },
  graphScrollView: {
    paddingTop: 10,
    marginBottom: 75
  },
  graphTitle: {
    fontSize: 18,
    fontWeight: "bold",
    margin: 10,
    textAlign: "center"
  }
}));
