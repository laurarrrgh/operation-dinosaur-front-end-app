import React from 'react';
import { Text, View } from 'react-native';

const Footer = ({
    params,
}) => (
    <View>
        <Text>Somebody once told me the world was gonna roll me</Text>
    </View>
);

export default Footer;
